import { eslintConfigWebsummit } from './eslint-config-websummit';

describe('eslintConfigWebsummit', () => {
  it('should work', () => {
    expect(eslintConfigWebsummit()).toEqual('eslint-config-websummit');
  });
});
